const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', err => console.log('PAGE ERROR:', err.toString())); try { await page.goto('https://volunteerny-app-v2.vercel.app/', { waitUntil: 'networkidle2' }); } catch (e) { console.error('Failed to load page:', e); } await browser.close(); })();
